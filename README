* My own reimplementation of unix ls utility.
* ls utility show list of files contained in directory.
* Implemented flags: -a -A -R -m -p -1 -C -o -g -d.
  Some flaxed can also be combined.
* Date of creating: 10.02.2020.

#QUICKSTART
Just write `make` and binary `uls` will be created.
Use: `./uls` 
